name: v0_zeroshot
description: Zero-shot baseline using Qwen 3 8B (newer than 2.5) via local Ollama.

llm:
  backend: ollama
  model: qwen3:8b
  base_url: http://localhost:11434
  timeout_s: 120

generation:
  temperature: 0.0
  max_tokens: 256
